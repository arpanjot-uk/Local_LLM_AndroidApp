<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/white"
    android:padding="0dp">


    <!-- Container for Notch Style Buttons -->
    <RelativeLayout
        android:id="@+id/notchContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginVertical="15dp"
        android:layout_marginHorizontal="15dp"
        android:background="@drawable/filled_background"
        android:elevation="10dp"
        android:padding="8dp">

        <!-- Ads Button (Left) -->
        <ImageButton
            android:id="@+id/idIBSkip"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentStart="true"
            android:layout_marginEnd="10dp"
            android:background="@drawable/main_rounded_button"
            android:hapticFeedbackEnabled="true"
            android:src="@drawable/ic_skip"
            android:tint="@color/white"
            tools:ignore="UseAppTint"
            android:contentDescription="Skip onboarding and go to the main app"/>


        <!-- Logo (Center) -->
        <ImageButton
            android:id="@+id/idIBLogo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_centerInParent="true"
            android:background="@color/humnod_shade"
            android:hapticFeedbackEnabled="true"
            android:src="@drawable/ic_humnod"
            tools:ignore="UseAppTint"
            android:contentDescription="HumNod Logo"/>

        <!-- Settings Button (Right) -->
        <ImageButton
            android:id="@+id/idIBNext"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:background="@drawable/main_rounded_button"
            android:hapticFeedbackEnabled="true"
            android:src="@drawable/ic_next"
            android:tint="@color/white"
            tools:ignore="UseAppTint"
            android:contentDescription="Proceed to the next onboarding screen"/>
    </RelativeLayout>

    <!-- ViewPager2 (Background for the onboarding screens) -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/onboardingViewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/notchContainer"
        android:layout_above="@+id/dotsIndicator"
        android:layout_marginVertical="5dp"
        android:layout_marginHorizontal="15dp"
        android:background="@drawable/hollow_background_4dp"
        android:elevation="8dp"
        android:contentDescription="Display onboarding screen"/>



    <!-- Dots Indicator (Bottom Center) -->
    <LinearLayout
        android:id="@+id/dotsIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:orientation="horizontal"
        android:layout_marginVertical="5dp"
        android:padding="8dp"
        android:elevation="10dp"
        android:contentDescription="Progress onboarding screen"/>
</RelativeLayout>